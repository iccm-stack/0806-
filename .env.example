GROQ_API_KEY=

# Optional: email delivery. The pipeline still creates the Markdown report
# when these values are omitted.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USERNAME=
SMTP_PASSWORD=
EMAIL_FROM=
EMAIL_TO=

# Set automatically by GitHub Actions; useful when running locally.
GITHUB_REPOSITORY=iccm-stack/0806-
GITHUB_SERVER_URL=https://github.com

