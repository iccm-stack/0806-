[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "global-ai-weekly"
version = "0.1.0"
description = "Automated, evidence-first global AI intelligence weekly"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
ai-weekly = "ai_weekly.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

